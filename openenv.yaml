name: smartops-agent
description: AI agent for ticket resolution

endpoints:
  reset: /reset
  step: /step

observation_space:
  ticket_id: str
  user_message: str
  category: str
  priority: str
  history: list
  time_elapsed: int

action_space:
  action_type: str
  content: str